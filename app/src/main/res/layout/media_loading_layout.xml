<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="horizontal" >

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center_vertical"
        android:orientation="horizontal" >

        <ProgressBar
            android:id="@+id/media_loading_p_id"
            style="@android:style/Widget.ProgressBar.Large"
            android:layout_width="138dp"
            android:layout_height="138dp"
            android:layout_centerHorizontal="true"
            android:indeterminateDrawable="@anim/cm_progress_anim"/>


        <TextView
            android:id="@+id/tv_speed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:textColor="#ffffff"
            android:singleLine="true"
            android:textSize="30sp" />
    </RelativeLayout>

</RelativeLayout>
    <!-- android:indeterminateDrawable="@drawable/icon_loading"
           <ImageView
                android:id="@+id/media_loading_image_bg"
                android:layout_width="200dp"
                android:layout_height="200dp"
                android:layout_gravity="center"
                android:src="@drawable/icon_loading_bg" /> -->
